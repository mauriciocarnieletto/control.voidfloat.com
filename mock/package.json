{
  "dependencies": {
    "json-server": "^0.16.3"
  },
  "scripts": {
    "start": "json-server -p 3000 ./db/void.json",
    "docker:run": "docker run --rm -d --network host --name json-server void/json-server",
    "docker:build": "docker build -t void/json-server .",
    "dev": "yarn start"
  }
}
