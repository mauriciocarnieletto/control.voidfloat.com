{
  "dependencies": {
    "json-server": "^0.16.3"
  },
  "scripts": {
    "start": "json-server -p 80 ./db/void.json",
    "docker:run": "docker run -p 80:80 -d void/json-server",
    "docker:build": "docker build -t void/json-server .",
    "dev": "yarn docker:build && yarn docker:run"
  }
}
